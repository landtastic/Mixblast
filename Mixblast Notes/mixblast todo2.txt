mixblast todo:

create playlist without loggin in
preview playlist without logging in
simplify


quickly create a youtube playlist:
(advanced options)

Enter a list of songs

create mix out of all youtubes on a page





function clearPlaceHolder(input) {
    if (input.value == input.defaultValue) {
        input.value = "";
    }
}

function setPlaceHolder(input) {
    if (input.value == "") {
        input.value = input.defaultValue;
    }
}